import { SelectCharacterFormPipe } from './select-character-form.pipe';

describe('SelectCharacterFormPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectCharacterFormPipe();
    expect(pipe).toBeTruthy();
  });
});
