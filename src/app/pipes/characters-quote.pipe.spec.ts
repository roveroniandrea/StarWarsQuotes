import { CharactersQuotePipe } from './characters-quote.pipe';

describe('CharactersQuotePipe', () => {
  it('create an instance', () => {
    const pipe = new CharactersQuotePipe();
    expect(pipe).toBeTruthy();
  });
});
